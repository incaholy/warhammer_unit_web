// Vitest global setup: register jest-dom matchers (toBeInTheDocument, etc.)
// and their type augmentation for expect. Wired via vite.config.ts `setupFiles`.
import '@testing-library/jest-dom/vitest'
